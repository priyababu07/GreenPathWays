// Get the "JOIN NOW" button element
const joinNowBtn = document.getElementById('joinNowBtn');

// Add a click event listener to the button
joinNowBtn.addEventListener('click', () => {
  // Redirect or perform some action when the button is clicked
  window.location.href = 'https://example.com/join'; // Replace with your desired URL
});